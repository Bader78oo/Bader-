# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Repository status

This repository is currently empty — it has no commits, source files, build configuration, or
documentation yet. There is no codebase to describe: no language/framework has been chosen, no
build/lint/test tooling is configured, and no architecture exists.

## Working in this repository

Because there is nothing here yet, do not assume a stack, directory layout, or tooling. When
the first real code is added:

1. Update this file with the actual build, lint, and test commands (including how to run a
   single test) once a project is scaffolded.
2. Add a "Code architecture" section describing the real structure once it exists — do not
   speculate about it beforehand.
3. If a `README.md`, `.cursorrules`/`.cursor/rules/`, or `.github/copilot-instructions.md` is
   added later, fold their key points into this file.
